# エラー表示

エラーをエンドユーザーに表示する際の規約。

## バックエンドのシステムメッセージをそのまま表示しない

バックエンドのレスポンスに含まれるエラーメッセージ(英語のドメインエラー `message` 等)を、トースト等でエンドユーザーにそのまま表示しない。エラーの種類(Connect の `Code` など)で分岐し、ユーザー向けの日本語メッセージを出す。

例外: 開発者向け画面など、原文をそのまま見せたい特殊なケースは意図を明示したうえで許容する。
