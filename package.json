{
  "private": true,
  "scripts": {
    "clean": "pnpm run -r clean && rimraf node_modules",
    "dev": "turbo run dev",
    "lint": "pnpm run -r lint",
    "format": "pnpm run -r format",
    "fix": "pnpm run -r fix",
    "tsc": "pnpm run -r tsc",
    "test": "pnpm run -r test",
    "test-ix": "pnpm run -r test-ix",
    "build": "pnpm run -r build",
    "lint:proto": "pnpm exec buf lint",
    "gen:proto": "pnpm exec buf generate"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.34.0",
    "@bufbuild/protobuf": "^1.10.0",
    "@bufbuild/protoc-gen-es": "^1.10.0",
    "@connectrpc/protoc-gen-connect-es": "^1.4.0",
    "@connectrpc/protoc-gen-connect-query": "^1.4.1",
    "rimraf": "^5.0.1",
    "turbo": "^1.10.13",
    "zx": "^7.2.3"
  }
}
